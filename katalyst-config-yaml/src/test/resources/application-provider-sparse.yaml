# Required keys plus a single application key: everything else must fall back to caller defaults.
ktor:
  deployment:
    host: 0.0.0.0
    port: 8080
    shutdownGracePeriod: 1000
    shutdownTimeout: 5000
database:
  url: jdbc:h2:mem:sparse
  username: sa
  driver: org.h2.Driver
app:
  name: Katalyst
