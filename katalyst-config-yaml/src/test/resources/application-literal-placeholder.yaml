# Regression fixture: the resolved secret legitimately contains a literal ${...} sequence.
# KATALYST_TEST_LITERAL_PLACEHOLDER is exported by the module's test task.
ktor:
  deployment:
    host: 0.0.0.0
    port: 8080
    shutdownGracePeriod: 1000
    shutdownTimeout: 5000
database:
  url: jdbc:h2:mem:literal
  username: sa
  driver: org.h2.Driver
  password: ${KATALYST_TEST_LITERAL_PLACEHOLDER:}
