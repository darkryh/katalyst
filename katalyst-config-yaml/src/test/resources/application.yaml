ktor:
  deployment:
    host: 0.0.0.0
    port: 8080
    shutdownGracePeriod: 1000
    shutdownTimeout: 5000

database:
  url: jdbc:h2:mem:base
  driver: org.h2.Driver
  username: test
  retries: 1
feature:
  enabled: false
shared: base
