# Build-logic is small; disable incremental compilation to avoid Kotlin cache corruption.
kotlin.incremental=false
# Avoid Kotlin daemon for build-logic to reduce cross-build cache contention.
kotlin.compiler.execution.strategy=in-process
