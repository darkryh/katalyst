<configuration>
    <property name="ROOT_LOG_LEVEL" value="${ROOT_LOG_LEVEL:-INFO}"/>

    <conversionRule conversionWord="highlight"
        class="ch.qos.logback.classic.pattern.color.HighlightingCompositeConverter" />

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <withJansi>true</withJansi>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %highlight(%-5level) %cyan(%logger{36}) - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>
    <root level="${ROOT_LOG_LEVEL}">
        <appender-ref ref="STDOUT"/>
    </root>

    <!-- Loggers deliberately inherit the root level. The embedded TUI inspector silences the
         console by raising the ROOT logger to WARN when it takes over the terminal — a logger
         pinned to an explicit level keeps that level and would keep printing over the dashboard.
         Pin a level only to suppress a noisy library (below) or temporarily while debugging:
           <logger name="io.github.darkryh.katalyst" level="DEBUG"/>
           <logger name="ServiceRegistry" level="DEBUG"/>
         Raise everything at once with -DROOT_LOG_LEVEL=DEBUG. -->
    <logger name="org.reflections" level="WARN"/>
</configuration>
